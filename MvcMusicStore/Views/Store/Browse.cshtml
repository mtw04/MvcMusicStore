@model MvcMusicStore.ViewModels.StoreBrowseViewModel

@{
    ViewBag.Title = "Browse Albums";
}

<h2>Browsing Genre: @Model.Genre.Name</h2>

<ul>
    @foreach (MvcMusicStore.Models.Album album in Model.Albums)
    {
        <li>@album.Title</li>
    }
</ul>
